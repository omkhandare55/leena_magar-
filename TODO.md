# TODO: Update Authentication Flow

## Tasks
- [ ] Update server.js: Remove auto-login logic from /api/auth/register endpoint
- [ ] Update public/js/auth.js: Change redirect from /dashboard.html to /login.html after registration

## Followup Steps
- [ ] Test registration flow: Ensure users are redirected to login page after registration
- [ ] Test login flow: Verify users can access dashboard only after successful login
